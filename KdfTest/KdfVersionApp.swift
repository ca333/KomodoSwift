import SwiftUI

/// The entry point for the Komodo DeFi Framework demo app.
/// It hosts a single `ContentView` in the root window group.
@main
struct KdfVersionApp: App {
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}